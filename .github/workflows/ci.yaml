name: 📦🚀 CI


on:
  push:
    branches:
      - '**'


permissions:
  contents: write
  pull-requests: write


# Serialise per-branch runs so a burst of merges can't pile up and exhaust the
# account's concurrent-job cap. On branches/PRs, cancel superseded runs for fast
# feedback; on main, queue instead of cancel so an in-flight PyPI publish is
# never killed mid-upload.
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: ${{ github.ref != 'refs/heads/main' }}


env:
  DEFAULT_PYTHON_VERSION: '3.13'


jobs:
  validation:
    name: 🧪 Validation
    # Skip on release-please's own release commit — it already ran pre-merge.
    # `startsWith`, not `contains`: the message includes the body, so a commit
    # that merely *mentions* the release prefix in prose would skip all checks.
    if: ${{ !startsWith(github.event.head_commit.message, 'chore(main)') }}
    strategy:
      fail-fast: false
      matrix:
        python-version: ${{ github.ref == 'refs/heads/main' && fromJson('["3.13"]') || fromJson('["default"]') }}
        os: ${{ github.ref == 'refs/heads/main' && fromJson('["ubuntu-latest", "macos-latest", "windows-latest"]') || fromJson('["ubuntu-latest"]') }}

    runs-on: ${{ matrix.os }}
    permissions:
      contents: read
      issues: none
      pull-requests: none


    steps:
      - uses: actions/checkout@v7

      - uses: corriander/gha/uv/test@v0.6.1
        env:
          UV_PYTHON: ${{ matrix.python-version == 'default' && env.DEFAULT_PYTHON_VERSION || matrix.python-version }}

  qa:
    name: 🛡️ QA
    # Formatting/lint/types are platform-independent, so this runs once rather
    # than across the validation matrix.
    if: ${{ !startsWith(github.event.head_commit.message, 'chore(main)') }}  # as validation
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v7

      - uses: corriander/gha/uv/qa@v0.6.1
        env:
          UV_PYTHON: ${{ env.DEFAULT_PYTHON_VERSION }}

  lock:
    name: 🔒 Lock
    # No release-commit skip here, unlike validation/qa: the release commit is
    # exactly where release-please's uv.lock bump (release-please-config.json,
    # via an undocumented jsonpath) could silently stop matching. This job gates
    # `release`, so a stale lock blocks the publish rather than shipping under a
    # green run. Its own job because the shared QA action's plain `uv run`
    # would rewrite a stale lock in the runner and mask the drift.
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v7

      - uses: astral-sh/setup-uv@v7
        with:
          enable-cache: true

      - name: Check uv.lock is current
        run: uv lock --check

  release:
    name: 🚀 Release
    runs-on: ubuntu-latest
    needs: [validation, qa, lock]
    permissions:
      contents: write
      pull-requests: write
      id-token: write  # for PyPI trusted publishing
    if: |  # always run on default branch as long as no failures, even if validation/qa are skipped (lock never is)
      always()
      && !contains(needs.*.result, 'failure')
      && github.ref == format('refs/heads/{0}', github.event.repository.default_branch)
    steps:
      # Config lives in release-please-config.json (manifest mode): pre-1.0,
      # a breaking change bumps the minor version rather than cutting 1.0.0.
      - uses: googleapis/release-please-action@v5
        id: release

      - uses: actions/checkout@v7

      - uses: astral-sh/setup-uv@v7
        if: steps.release.outputs.release_created

      - name: Build
        if: steps.release.outputs.release_created
        run: uv build

      - uses: svenstaro/upload-release-action@v2
        if: steps.release.outputs.release_created
        with:
          repo_token: ${{ secrets.GITHUB_TOKEN }}
          file: dist/*
          tag: ${{ steps.release.outputs.tag_name }}
          file_glob: true
          make_latest: false

      - name: Publish to PyPI
        if: steps.release.outputs.release_created
        run: uv publish --trusted-publishing always
